-> First run the prprocessing.py:

	Run it for both the customer_information as well as for balance_information files.

-> Next update the result data into cassandra and hive tables respectively.

-> Extract the customer_data using the following query and export it into excel and place it in this Folder( folder with the scripts).

-> Finish all of this by EOD of 1st (7pm) or the last working day of the prior month.

-> The push notifications will automatically be sennt at 8pm on 1st of ever monthy.

-> you can view all the customar stats in heu using the data in the hive tables.